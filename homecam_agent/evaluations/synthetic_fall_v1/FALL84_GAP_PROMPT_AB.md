# 84개 원인별 비교 — 2026-09-17

사용자가 승인한 순서: 대상 연결 확인 → 짧은 검출 끊김 처리 비교 →
Gemma 프롬프트 비교. 기존 결과·라벨·박스·운영 기본값은 바꾸지 않는다.
이미 검토하고 수정에 사용한 합성 개발 데이터이며 독립 검증이 아니다.

## A. 같은 Pose 출력에서 재등장 기록 유지 비교

- 기준: eval84-gemma-20260917.fiHWKe/pose-reference (pose-loss + 요청 중복 억제).
- 동일한 2,224개 실제 ONNX 출력과 트래커 결과를 사용한다. 새 추론이나 ID 보간 없음.
- 기존 `PoseGapConfig.resume_after_missing=True`를 재평가한다. 41개 실험 당시
  정상 영상 요청도 늘어 비활성화했던 옵션이므로 새 알고리즘이라고 부르지 않는다.
- 0.5초 이내 명시적인 missing만 허용하고 다시 관측된 같은 트랙의 낮은 자세를 연결한다.
  모호한 연결·미할당 Pose·관절 부족·다른 자세·트랙 만료는 끊는다.
- 실제 인접 관측 간격의 합 0.6초 기준은 유지한다. 누락 시간을 더하지 않는다.
- 기하·신뢰도·모델·샘플링·깊이·로봇 움직임·기존 마지막 관측 요청은 그대로 유지한다.
- 요청 중복 억제도 같은 규칙으로 적용한다. 변경이 없는 대조군 재생 결과도 검증한다.
- 영상 단위 호출 유무와 실제 대상 연결을 구분한다. GT 상자가 없는 시점은 보간하지 않는다.
  실제 RGB와 출력 상자를 검토하되 확인할 수 없는 연결은 unknown으로 남긴다.
- 정상 영상 증가, 요청 수, 첫 후보 시각을 함께 보고한다. 배포·최적 임계값 선정 없음.

## B. Gemma 판단 문구만 변경

- 기준: vlm-run-r3/gemma4--12b의 완료된 full 84건, gated 39건 호출 및 45건 미호출.
- 같은 gemma4:12b digest, 양자화, 옵션, 스키마, RGB JPEG, 순서, 시각, 사용자 문구를 유지한다.
- 원본 영상에서 입력을 재구성해 기존 프레임 해시 및 전체 요청 해시와 일치하는지 검사한다.
- system prompt에만 정상 판정에 필요한 관측 근거 및 불확실성 구분 지시를 추가한다.
  출력 형식과 채점 기준은 변경하지 않는다. 클립 ID·정답·판단 이유·센서 요약은 보내지 않는다.
- A에서 달라진 후보를 B에 적용하지 않는다. 첫 실험에서는 두 변경의 효과를 섞지 않는다.
- 실제 무료 로컬 호출만 수행한다. 클라우드·새 모델 다운로드·자동 재시도 없음.
- 전체 84개 및 기존 gated 호출 39개 모두 평가한다. 실패도 분모에 남긴다.
- 각 호출 결과를 즉시 저장한다. 중단 시 부분 건수만 공개하고 전체 정확도를 계산하지 않는다.
- B는 저장된 입력을 재전송하는 비교다. 새로운 실시간 Pose 실행이나 로봇 종단 간 지연이 아니다.
- 응답 시간은 참고로 남긴다. 온도·메모리·모델 적재 순서가 통제된 지연 비교는 아니다.
- 추가 문구의 효과는 이번 실행으로 평가하며, 잘못된 정상 판단이 반드시 줄어든다고 가정하지 않는다.
