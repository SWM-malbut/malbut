# 같은 입력의 로컬 VLM 교체 비교 (2026-09-17)

- 첫 비교 모델: 이미 설치된 `qwen2.5vl:7b`. 기존 41개 평가에서 다중 이미지 호출을 완료한
  다른 계열 모델이다. 당시의 4분류/36개 정확도를 현재 84개/3분류 결과와 섞지 않는다.
- 대조군: 원래 Gemma4:12b A. 성능 악화로 기각한 추가 문구 B와 출력 분리 실험은 사용하지 않는다.
- 모델 교체 이외에 system/user 본문·12장 JPEG·시각·구간·옵션·출력 스키마·채점 기준 유지.
  thinking 미지원 모델에서는 Gemma의 `think=false` 필드만 생략한다. 모델 고유 토크나이저,
  이미지 처리 및 템플릿은 모델 교체의 일부다. 동일 픽셀 입력이 동일 내부 토큰을 뜻하지 않는다.
- 같은 후보 40건(기존 39 + V034 f077) 먼저, 전체 84건 다음. 124회 실제 로컬 호출.
  YOLO 재추론·후보 추가·라벨 수정·ROI·미래 프레임 추가 없음. GT/이유/센서 정보 전송 없음.
- 과거 Gemma 124개 요청을 첫 추론 전에 모두 재구성해 검증. Qwen의 요청도 같은 구성임을 확인.
- 기존 옵션: temperature=0, seed=0, num_ctx=16384, num_predict=1200. thinking은 끈 상태 또는
  미지원. 요청 제한 300초, 자동 재시도/새 모델 다운로드/유료 호출 없음.
- 모델/서버/입력/코드/설정 해시 고정. 각 요청·응답·결과를 즉시 보존. 중단 후 덮어쓰기 없음.
- HTTP 오류 등 호출 라이브러리가 중단하는 오류는 저장 후 중단. 시간 초과 등 일반 실패는
  실패로 남기며 미호출/실패를 정상으로 바꾸지 않는다. 응답 형식 실패도 분모에서 제외하지 않는다.
- 주 지표: 낙상·의심을 확인 대상으로 남긴 수, 정상 오탐, 정상으로 놓친 낙상·의심,
  3분류 정확도와 형식 유효성. gated 분모40, full 분모84. 의심25개도 평가에 포함한다.
- 지연은 Mac/SSH 요청 지연. 실제 Jetson의 동시 실행·낙상 발생부터 조치까지 시간이 아니다.
  prompt_eval_count 등 토큰 사용량도 보존하여 컨텍스트 제한 근접 여부를 점검한다.
- 실행 중 모델별 문구 조정 금지. 이 데이터는 이미 검토·개선에 사용한 합성 개발 데이터이며,
  독립 성능 검증/임상 안전성/운영 배포 승인으로 해석하지 않는다.
