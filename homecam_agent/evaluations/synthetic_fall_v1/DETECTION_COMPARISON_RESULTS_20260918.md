# 검출 비교 결과와 다음 검증 모델

## 결정

2026-09-18 사용자와 **YOLO26s-Pose + 원본 비율 유지**를 다음 검증의 우선 후보로 정했다.
n 모델의 기존 방식과 비율 유지 방식은 비교용으로 남긴다. 전체 성능이 가장 좋다고 확정하거나
로봇에 배포한 결정은 아니다. 설정과 가중치 해시는 `DETECTION_CANDIDATE_SELECTION.json`에 남겼다.
운영 설정, 승인된 라벨·박스, 판단 임계값은 바꾸지 않았다. VLM은 호출하지 않았다.

## 평가 범위와 결과

- 합성 영상 84개: 낙상 25 / 낙상 의심 25 / 정상 34.
- 원본 해상도는 **640×400 41개 + 1280×720 43개**다. 전부 카메라의 640×400 영상은 아니다.
- 동일한 2,224프레임을 네 조건으로 실제 추론: 8,896회, 워밍업 별도 2회.
- 최종 사용자 승인 박스 533개를 사용했다. 검수하지 않은 프레임의 박스를 보간하지 않았다.
- 추적·자세 판단·짧은 검출 끊김 처리·중복 요청 조건은 모두 고정했다.
- 이미 보고 개선한 개발 데이터다. 미사용 영상이나 실물에서의 일반화 성능을 입증하지 않는다.

| 조건 | 낙상 영상 요청 | 의심 영상 요청 | 정상 영상 요청 | 검수 박스 대응 | 평균 / p95 처리 시간 |
| --- | --- | --- | --- | --- | --- |
| n + 기존 비율 변형 | 17/25 | 15/25 | 10/34 | 430/533 | 52.3 / 66.1 ms |
| n + 비율 유지 | 18/25 | 18/25 | 11/34 | 448/533 | 51.0 / 55.6 ms |
| s + 기존 비율 변형 | 17/25 | 15/25 | 12/34 | 466/533 | 135.9 / 147.8 ms |
| s + 비율 유지 | 21/25 | 16/25 | 13/34 | 480/533 | 134.7 / 138.3 ms |

위 요청 수는 영상 내 요청 발생 여부다. **대상자·시점 검수를 끝낸 낙상 재현율이나
VLM 정확도가 아니다.** 정상 영상 요청은 불필요한 확인 요청이며 확정 알림과 다르다.
박스 대응은 기존 기준(보이는 정답 영역 50%, 예측 영역 25%, 중심 포함, 유일 대응)을 썼다.
관절 정답이 없으므로 관절 위치 정확도도 아니다.
시간은 같은 PC의 CPU/ONNX Runtime에서 전처리·추론·좌표 복원만 측정했다.
영상 디코딩, VLM, 음성, 주행을 포함한 지연이나 Jetson 측정값이 아니다.

n + 비율 유지 대비 s + 비율 유지는 낙상 3개 증가, 의심 2개 감소다.
확인 대상 50개를 합하면 요청 영상 수는 36→37, 정상 요청은 11→13이다.
처리 시간은 약 2.6배이므로 무조건적인 우위로 해석하지 않는다.

### 640×400 영상만 분리

| 조건 | 낙상 14개 | 의심 7개 | 정상 20개 |
| --- | --- | --- | --- |
| n + 기존 비율 변형 | 10 | 3 | 3 |
| n + 비율 유지 | 9 | 2 | 4 |
| s + 기존 비율 변형 | 10 | 4 | 4 |
| s + 비율 유지 | 13 | 3 | 5 |

이 표도 요청 발생 수다. 작은 표본이고 해상도별 영상 내용도 다르다.
해상도만 바꾼 실험이 아니므로 해상도 자체의 효과로 해석하지 않는다.

## 실제 이미지로 확인한 변화

네 조건 비교 JPG와 요청의 실제 프레임·track ID·판단 이유를 대조했다.
아래는 s + 비율 유지와 n + 기존 비율 변형 사이에서 요청 유무가 바뀐 **14개 영상**에 대한 검토다.
108의 중복 요청도 별도로 확인했다. 모든 84개 사건의 올바른 대상·시점 판정을 완료한 것은 아니다.
이 검토는 도구 출력의 사후 검토이며, 사용자 라벨·박스를 수정하지 않는다.

### 새로 요청한 낙상·의심 영상

| 영상 | s 첫 요청 | 이미지 및 출력 확인 |
| --- | --- | --- |
| 404 | f043 / 3.583초 | 의자 다리에 가려 바닥으로 누운 실제 대상자에 검출이 이어진다. n은 해당 구간 검출이 사라진다. |
| 422 | f031 / 2.583초 | 탁자 아래 넘어져 있는 대상자 ID1에서 요청한다. 주변 가구에도 별도 검출이 있어 모두 사람으로 해석하면 안 된다. |
| V001 | f077 / 3.208초 | 넘어진 뒤 바닥의 실제 대상자 ID2에서 요청한다. 이전 n의 박스는 상체 일부로 줄어든다. |
| V009 | f062 / 2.583초 | 앞으로 쓰러진 실제 대상자 ID1에서 자세 변화 요청을 만든다. |
| V031 | f019 / 0.792초 | 탁자 아래 누운 대상자의 상체부터 다리 쪽까지 검출이 붙는다. |
| V033 | f019 / 0.792초 | 카메라에 등을 보이며 누운 실제 대상자에 검출과 관절 출력이 붙는다. |
| V037 | f019 / 0.792초 | 역광과 가구 가림이 있는 실제 누운 대상자에 검출이 붙는다. |
| 413 | f010 / 0.833초 | 침대·이불 영역의 ID2를 `found_down`으로 요청한다. 보이는 머리 검출과 겹친 별도 큰 박스이며, 가려진 몸의 관절을 검증할 수 없다. 바닥에 떨어진 뒤 잡았다고 확정하지 않는다. |

새 요청 8개 중 **7개는 실제 바닥의 대상자 위치에 출력이 붙는 것을 확인**했다.
이는 낙상 확정 또는 새 정답 라벨이 아니다. 413은 별도 보류한다.
413의 요청은 기존 낙상 시작 범위 f007~013 안에 있지만, 바닥에서 처음 보이는 범위
f023~029보다 빠르다. 침대 위 누운 자세에 대한 요청을 낙상 이후 검출 개선으로 세면 안 된다.
V031·V033·V037은 승인된 시간 라벨이 없어 정확한 감지 지연은 계산하지 않았다.

낙상 시작 범위가 있는 위 네 사례의 요청 지연은 다음과 같다.
이는 **영상 시각 기준**이며 모델 실행에 걸린 벽시계 시간까지 포함한 실시간 지연이 아니다.

| 영상 | 승인된 시작 범위 | 요청까지의 영상 시간 |
| --- | --- | --- |
| 404 | f029~033, 12fps | 0.833~1.167초 |
| 422 | f011~018, 12fps | 1.083~1.667초 |
| V001 | f044~050, 24fps | 1.125~1.375초 |
| V009 | f034~040, 24fps | 0.917~1.167초 |

### 기존 요청을 놓친 세 영상

| 영상 | 확인한 원인 |
| --- | --- |
| V023 | 바닥의 사람과 옆 사람이 겹친다. 검수 프레임 6개 중 대상자와 유일 대응한 출력은 3→2개, 대응이 애매한 프레임은 3→4개다. 낮은 점수·중복 검출·추적 끊김을 함께 봐야 한다. 단순히 사람 박스가 없는 문제는 아니다. |
| V027 | 대상자 박스 대응은 오히려 5→6개지만 자세 계산 가능한 대응은 5→1개다. s에서는 f000~096 검수 구간의 몸통 관절이 충분하지 않아 자세를 계산하지 못한다. 마지막 f120에서만 계산 가능한 자세가 생겨 지속 조건을 채우지 못한다. |
| V048 | f072·101·106에서 검출이 없고, 그 사이 검출된 자세도 낮은 자세 조건을 충분히 이어가지 못한다. f077에서 낮은 자세 1개를 모은 뒤 f082에서 초기화된다. 기존 n은 f082~101에서 5개를 모아 f101에 요청한다. |

V048은 ID가 바뀌어서만 생긴 문제가 아니다. 같은 ID1에서도 검출 끊김과
관절 배치·몸 축 계산 결과가 함께 달라진다. 박스 검출 점수만 낮추는 것으로 해결됐다고 볼 수 없다.

### 새 불필요 요청과 중복

- **425:** 고양이와 방석 영역에 사람 관절을 출력하고 f010에 요청한다. 실제 사람이 없는 오검출이다.
- **55:** 요 위에서 자는 사람의 이불 영역까지 자세를 출력해 f010에 요청한다. 정상 수면 라벨을 유지한다.
- **V039:** 사용자가 정상으로 확정한 눕는 동작에서 f101에 요청한다. 대상자를 잘못 고른 경우가 아니라 정상 자세에 대한 요청이다.
- **108:** 기존 n의 요청 1회가 s에서는 f010과 f053의 2회가 된다. 한 사람에 전신/부분 검출이 나뉘는 문제가 새 출력에서도 보인다. 기존 모델에서 중복 처리가 통과했다고 새 모델에서도 해결됐다고 하면 안 된다.

s의 정상 요청은 영상 수 13개, 요청 건수 14개다. 이 둘을 혼동하지 않는다.

## 검증 상태

- 완료 결과, 네 조건의 산출물, 최종 공간 라벨 해시 확인.
- n 대조군의 실제 Pose·추적·기본 판단 및 최종 요청 결과가 기존 저장 결과와 일치.
- 관련 단위 테스트 216개 통과.
- 설치된 Ultralytics의 LetterBox와 다섯 원본 크기에서 픽셀 단위 일치.
- n/s PyTorch와 ONNX를 실제 세 프레임 × 두 입력 방식, 총 12조건에서 대조하여 통과.
  confidence 0.10 이상 출력 개수와 좌표·관절 값 허용 오차를 확인했다.
- 네 조건 비교 페이지 생성. 모든 요청 프레임과 과거 관측 참조를 포함하고, 미검수 GT는 그리지 않는다.
- 실물 Jetson 동시 실행, 실제 640×400 카메라 영상, 오디오/depth, VLM 재평가는 미실행.
- 모든 84개 영상의 대상자·시점 감사와 최종 재현율 산출은 미완료다.

## 다음 순서

1. s 후보와 고정된 n 비교군을 유지한다. 이번 결과에 소급하여 임계값을 바꾸지 않는다.
2. V023·V027·V048의 추적/관절 가림 문제와 425·108의 오검출/중복을 별도 개선 실험으로 다룬다.
   특정 영상 ID를 조건에 넣거나, 누워서 움직이지 않는 사람을 제거하는 방식은 사용하지 않는다.
3. 로봇 연결 후 현재 실행 상태를 먼저 확인하고, 물리 이동 명령 없이 저장 영상으로
   Jetson의 처리 지연·메모리·CPU/GPU·주행/음성 부하 영향을 측정한다.
   모델 패키지 설치나 운영 서비스 변경이 필요하면 범위를 먼저 확인한다.
4. 대상·시점 검수 및 실물 자원 확인 후 채택 여부를 결정한다.

## 결과 위치

- 결과 루트: `/home/jisanggeun/.local/share/malbut-evaluations/fall84-detection-comparison-20260917-r2`
- 요약: `summary.json`, SHA256 `dddd1d2674a16a626e4fb32b47cbde813388f6a2f84928250972814c1697ace7`
- 비교 이미지/페이지: `visuals-v2/index.html` (초록=최종 GT, 파랑=모델 출력)
- 해상도별 집계/이미지 출처: `visuals-v2/review.json`
- 내보내기 대조: `export-parity.json`
- 프레임별 출력: 각 조건의 `frames.jsonl`; 실제 요청 시각은 `dispatches.jsonl`.
- 사용자 승인 공간 라벨 freeze SHA256: `c8bbb590e9e48b88969b1610c1fa3d844ef77d8d63095352fd3d72a6eb79a03a`
