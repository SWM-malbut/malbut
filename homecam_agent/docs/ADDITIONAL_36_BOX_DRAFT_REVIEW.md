# 추가 36개 영상 사람 박스 검토 완료

2026-09-16 사용자 최종 확인 완료. 최종 평가 GT에 병합하는 작업은 별도다.

## 범위

- 추가 채택 영상 36개(SYN042~SYN077). V013·V036·V038은 제외 유지.
- 영상마다 0·24·48·72·96·120프레임: 총 216프레임, 239박스.
- 좌표는 원본 1280×720 기준 `[frame, x1, y1, x2, y2]`이다.
- 보이는 몸의 외곽을 표시했다. 가려진 몸이나 화면 밖 부위는 추정하지 않았다.
- V004·V023은 사람 2명, V027은 3명으로 분리했다. ID는 영상 안에서만 유효하다.
- V035의 0프레임은 사람이 보이지 않아 박스 없이 `not_visible_frames`에 기록했다.
- 기존 41개의 박스·시각 라벨, 77개의 분류 라벨 및 평가 묶음은 변경하지 않았다.

## 검토 자료

현재 Linux 위치:

```text
/home/jisanggeun/.local/share/malbut-evaluations/boxes36-20260912.gxbAu5/
  추가36개_사람박스_검토초안_20260916/
    열어보기.html
    JPG/                         # V 번호별 검토 이미지 36장
    자료/                        # 좌표 JSON, 원본 프레임, 해시 목록
```

HTML에서 영상 선택·원본 비교·개별 프레임 확대가 가능하다. 좌표를 직접 수정하는
편집기는 아니다. 수정할 부분은 `V번호 · 프레임번호 · P번호 · 수정 내용`으로 받는다.
박스 색과 P번호는 사람 구분용이며 모델 검출 결과나 낙상 확정을 의미하지 않는다.

Mac 바탕화면의 `추가36개_사람박스_검토초안_20260916/`에 수정본을 복사했고,
292개 파일의 해시를 확인했다. `열어보기.html`을 열면 된다.
ZIP은 사용자 요청으로 Linux 휴지통에 옮겼다. 원본 영상·좌표는 보존했다.

## 1차 수정

- V005 f048·072: 아래쪽 여백 축소.
- V009 f048·072: 책상 다리 너머 보이는 다리·발 포함.
- V020 f072: 왼쪽에 빠진 머리 포함.
- V033 f048·072: 양말에 맞게 오른쪽·아래 경계 축소.
- V034 f072: 위쪽 몸 외곽 포함.
- V037 f048·072·096·120: 어깨 포함. f082는 사용자가 f072라고 확인했다.

수정한 12개 박스 외 원본 프레임·라벨·대상자 ID는 동일하다.
1차 좌표는 작업 폴더의 `review-corrected-20260916-r1/annotations.json`이며,
수정 내역은 `USER_REVIEW_CORRECTIONS_R1.md`에 남겼다.

## 2차 수정

- V009 f096·120: 책상 다리 너머 보이는 발까지 오른쪽 경계를 확장했다.
- 두 박스의 원본 좌표 x2를 각각 574·576에서 636으로 변경했다.
- 나머지 박스는 유지했고, 위 두 수정 박스까지 사용자 최종 OK를 받았다.
- 현재 좌표: `review-corrected-20260916-r2/annotations.json`.
- `USER_REVIEW_CORRECTIONS_R2.md`에 확인 범위와 수정 내역을 기록했다.
- 수정 이미지를 다시 확인했고, 관련 테스트 13개 및 두 박스 외 불변 검사를 통과했다.
- Mac의 동일한 검토 폴더에 반영하고 292개 파일 해시를 다시 확인했다.

## 확인한 것과 남은 것

- 실제 36개 영상 SHA256 및 고정 라벨 메타데이터 해시 일치.
- 추출한 216프레임을 직접 확인해 좌표 초안 작성. YOLO/VLM 예측을 정답으로 복사하지 않음.
- 영상·대상·좌표 범위·프레임 수·라벨 불변 검사와 관련 테스트 56개 통과.
- 수정본 292개 내용 파일 해시를 Linux와 Mac 양쪽에서 검증.
- Firefox 헤드리스에서 검토 화면의 실제 렌더링 확인.
- 36개 영상의 216개 대표 프레임, 239개 박스 모두 사용자 확인 완료.
- 승인 기록은 작업 폴더의 `BOX_REVIEW_APPROVAL.json`이며, 승인한 좌표 파일 SHA256에 연결했다.
- 검토 도구의 draft JSON은 그대로 보존했다. 최종 평가 GT 병합은 아직 하지 않았다.
- 1초 간격의 대표 프레임만 표시했으며, 낙상 시작 시각·모든 프레임의 위치 정답은 추가하지 않았다.
- 모델 성능 재평가·API 호출·커밋·PR·로봇 배포는 이번 작업에 포함하지 않았다.

생성 도구는 `scripts/review_additional_boxes.py`, 검증 테스트는
`test/test_review_additional_boxes.py`다. 수동 좌표 원본과 이전 초안은 작업 폴더에 보존한다.
