# 84개 평가 입력 고정 — 2026-09-17

## 완료한 내용

사용자가 승인한 라벨·판단 이유·박스·시간 구간을 새 묶음에 모았다.
기존 77개 묶음과 원본 영상, 검토 이력, 이전 모델 평가 결과는 변경하지 않았다.

| 라벨 | 영상 수 |
| --- | ---: |
| 낙상 | 25 |
| 낙상 의심 | 25 |
| 정상 행동 | 34 |
| 합계 | 84 |

기준은 `fall-evaluation-v2-20260912-r1` 그대로다. 라벨 3개 모두 채점하며,
낙상 의심을 분모에서 빼지 않는다. 이번 단계는 입력 고정이며 **모델 평가나 감지 지연 실측은 하지 않았다.**
이미 검토·개선에 사용한 합성 개발 데이터다. 독립 시험 세트나 실물 성능 증명이 아니다.

## 반영 내역

기존 SYN001–077 번호와 라벨은 유지했다. V026은 정상 라벨과 기존 이유를 보존하면서
“다시 확인했을 때, 눕는 동작과 자세가 유사해 보임.”을 추가했다.
V049는 최종 사용자 판단인 낙상으로 반영했다. 이전 의심 라벨은 검토 이력에 남아 있다.

| 검토 번호 | 평가 번호 | 최종 라벨 |
| --- | --- | --- |
| V040 | SYN078 | 낙상 |
| V043 | SYN079 | 낙상 |
| V044 | SYN080 | 낙상 |
| V045 | SYN081 | 낙상 |
| V048 | SYN082 | 낙상 |
| V049 | SYN083 | 낙상 |
| V050 | SYN084 | 낙상 |

V041·V042·V046·V047·V051은 품질 문제로 제외했다. 파일을 삭제하지 않았으며 분모에 포함하지 않는다.
기존 제외 기록도 그대로 유지했다.

- 박스: 원본 41개 + 승인된 추가 43개. 총 528개 박스, 이 중 이번에 합친 박스는 281개다.
- 추가 43개는 f000·024·048·072·096·120만 반영했다. 중간 프레임은 보간하지 않았다.
- SYN075(V035) 첫 프레임은 기존 `not_visible_frames=[0]`을 유지했다.
- 새 7개는 승인된 유일한 사람 P1을 대상으로 연결했다. 별도의 인물 ID 검증을 받았다는 뜻은 아니다.
- 시간: 기존 낙상 14개의 기록을 보존하고 승인된 11개를 추가했다.
- 낙상 시작: 25개 모두 구간으로 준비됨. 몸의 첫 바닥 접촉: 21개 확인, 기존 4개는 가려져 확인 불가.
- 시간 구간을 중간값으로 줄이지 않았다. 새 11개의 `first_down_frames`는 검토하지 않았으므로 null이다.
- 원본 초안의 pending 표시를 고치지 않고, 해시로 연결된 사용자 승인 기록을 함께 적용했다.

## 저장 위치

Mac:

```text
~/service/datasets/synthetic_fall_v1/evaluation_sets/fall84-v2-r1-20260917/
```

- `media/`, `media.json`: 중립적인 SYN 번호의 영상·입력 목록. 라벨·판단 이유·생성 정보는 넣지 않았다.
- `evaluation_labels.json`: 최종 라벨·이유·박스·시간 구간. 평가 전용이며 모델에 보내지 않는다.
- `inventory.csv`: 사람용 목록. V026의 추가 이유도 포함한다.
- `source_records.json`: 이전 묶음의 메타데이터와 이번 검토 원문·승인·해시를 보존한다.
- `scoring_criteria_v2*.json`: 기존 채점 기준을 바이트 그대로 보존한다.
- `protocol.md`, `validation.json`, `freeze.json`: 사용 범위·검증 결과·고정 해시.

원본 바이트를 그대로 복사했으므로 추가 43개 영상에는 음성 트랙이 남아 있다.
평가 입력은 RGB 전용이며, 실제 모델 호출에서도 음성을 제외해야 한다.

`freeze.json` SHA-256:

```text
8cb8a2c97906cfccb84f040919951abc61bed0ee8da7864354d51c2ede0a34a2
```

Linux 메타데이터 사본:
`/home/jisanggeun/.local/share/malbut-evaluations/freeze84-20260917.mQxbGX/metadata/`

## 확인한 결과

- Mac에서 84개 영상을 FFmpeg로 끝까지 디코딩: **7,871프레임**, 오류 없음.
- 원본/복사본 해시, 프레임 수, 해상도, FPS, 프레임 타임스탬프를 대조했다.
- 파일 바이트 또는 전체 디코딩 결과가 완전히 같은 영상은 없었다.
- 기존 77개는 영상 메타데이터와 디코딩 해시까지 새 묶음과 완전히 같았다.
- 동일 생성 장면 그룹 SYN005·SYN006은 표시를 유지했다. 비슷한 장면의 독립성까지 증명한 것은 아니다.
- 최신 Mac 라벨·인덱스·승인 기록과 복사한 검토 자료의 해시가 일치했다.
- 제외 영상이 다른 이름으로 편입되지 않았는지, 박스 좌표·대상 ID·시간 구간이 맞는지 검사했다.
- 기존 검증 진입점이 77개와 84개를 구분하도록 확장했다. 기존 77개 검증 조건은 유지했다.
- 관련 자동 테스트 **161개 통과**. 이 중 입력 고정 테스트는 가짜 미디어를 사용하며,
  위의 84개 실제 영상 디코딩 검사와 구분한다.
- 기존 VLM 실행기의 v2 라벨 사전 검사도 통과했다. provider 호출은 하지 않았다.
- 새 묶음의 메타데이터와 영상 권한은 0600이다. 기존 경로에 덮어쓰지 않는다.

사용한 코드: `homecam_agent/scripts/prepare_fall_evaluation_84.py`.
검증 진입점: `prepare_fall_evaluation_v2.py --verify <묶음 경로>`.

## 다음 단계

YOLO-Pose / VLM 단독 / YOLO-Pose 후 VLM의 실행 조건을 정하고 재평가한다.
시각 GT가 준비된 것과 실제 감지 지연이 측정된 것은 다르다.
시간순 재생과 대상 인물 연결을 검증한 뒤 시작 시각 구간에 대한 지연을 계산하고 미탐 수를 함께 보고해야 한다.
전체 영상을 미리 본 VLM의 처리 시간을 온라인 감지 지연으로 쓰지 않는다.

모든 라벨의 시간 검토가 끝난 것은 아니므로 기존 전체 시간 채점기의
`whole_dataset_target_and_timing_metrics_ready=false` 보호 조건은 유지했다.
25개 낙상 시작 구간을 사용하는 측정 경로는 이 범위에 맞춰 별도로 연결해야 한다.
모델 선택·샘플링·타임아웃, 추론 실행, 커밋·PR은 이번 작업 범위에 포함하지 않았다.
