# 낙상 감지 지연 측정 준비 — 2026-09-17

## 현재 상태

사용자가 11개의 시간 구간을 확인했다(“맞아. 잘했어.”).
`TIMING_REVIEW_APPROVAL.json`에 검토본 해시와 승인 범위를 기록했으며, 구간 값과 불확실성 범위는 그대로 유지한다.
검토 당시 초안 파일은 이력으로 보존한다. 새 84개 평가 입력 묶음에 승인 기록과 함께 반영했다.

채택 영상은 84개(낙상 25 / 낙상 의심 25 / 정상 34)다.
라벨·판단 이유·박스와 **새로 작성한 11개 영상의 시간 구간은 사용자 확인 완료**다.
새 84개 평가 묶음 고정을 완료했다. [묶음 구성과 검증 결과](FALL_EVALUATION_FREEZE84_20260917.md).
YOLO/VLM 재평가와 감지 지연 실측은 아직 하지 않았다.

- 기존 77개 중 낙상 14개에는 시작 시각 범위가 있다. 기존 기록을 그대로 옮겼다.
- 그중 4개는 첫 몸 접촉이 가려져 있어 확인 불가를 유지했다.
- 시간 표시가 없던 기존 V001·V002·V009·V019와 신규 V040·V043·V044·V045·V048·V049·V050, 총 11개에 시작·접촉 범위를 작성하고 사용자 확인을 받았다.
- 모든 84개가 낙상 지연 측정 대상이라는 뜻은 아니다. 정상·의심 영상은 분류·오탐 평가에 그대로 포함한다.

## 검토 방법

Mac 바탕화면 `낙상시각_11개_검토_20260917/열어보기.html`.

영상 선택, 한 프레임 이동, 시작/접촉 범위 앞뒤 이동, 원본 MP4 재생을 지원한다.
`JPG` 폴더에는 비교 사진 11장만 있고 JSON·CSV·원본 프레임·MP4는 `자료`에 모았다. ZIP은 없다.
수정할 때는 `V040 시작 f036–048 → f…–…`처럼 알려준다. 페이지에서 자동으로 파일을 저장하지 않는다.

## 표시 기준

- 시작: 낙상으로 이어지는 몸의 기울어짐·하강이 처음 보이는 경계. 준비 동작과 구분하기 어렵다면 범위로 남긴다.
- 몸 첫 접촉: 손·무릎이 아닌 골반 또는 몸통이 바닥/바닥 매트에 처음 닿는 경계. 완전히 눕거나 움직임을 멈추는 때와 다르다.
- 프레임 번호는 0부터 센다. 이번 11개는 모두 24fps·123프레임이며 실제 PTS와 원본 해시를 대조했다.
- 범위는 동작의 길이가 아닌 경계의 불확실성을 나타낸다. 중간값을 단일 정답으로 바꾸지 않는다.
- 생성 프롬프트나 모델 예측은 시각 정답을 만드는 데 사용하지 않았다.

## 측정할 때

시작 구간이 [a,b], 첫 후보 시각이 d이면 지연 구간은 [d-b,d-a]다.
시작 전 후보를 0초로 바꾸지 않고 사건과 관련된 후보인지 확인한다. 미탐 영상의 수를 지연 통계와 함께 보고한다.
후보 발생 시각, 모델 요청→응답 소요 시간, 필요한 영상 수집 시간을 포함한 최종 결과 도착 지연은 구분한다.
전체 영상을 먼저 본 분류 결과를 실시간 조기 감지로 해석하지 않는다.

## 검증 및 보존

- 검토 자료 검사 13개 통과: 원본 해시, 프레임 범위, PTS, 승인 전 상태, 화면 코드, 프레임 이동·영상 선택 등.
- Firefox에서 오프라인 검토 화면의 실제 렌더링 확인.
- 분류 라벨·박스·원본 영상·기존 77개 고정 평가 묶음은 변경하지 않음.
- 모델 호출·감지 지연 실측은 실행하지 않음. 승인된 시간 구간은 새 84개 평가 묶음에 반영 완료.

작업 자료: `/home/jisanggeun/.local/share/malbut-evaluations/timing84-20260917.sVxzeI/`.
Mac 기록 위치: `service/datasets/synthetic_fall_v1/annotations/timing_review_20260917/`.
