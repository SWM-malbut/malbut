# 77개 평가 자료 준비 완료

2026-09-12. 사용자 요청의 1번, 평가 자료 묶음 준비까지 완료했다. 모델은 실행하지 않았다.

Mac 위치:

```text
~/service/datasets/synthetic_fall_v1/evaluation_sets/fall77-v2-r1-20260912/
```

| 라벨 | 수량 |
|---|---:|
| 낙상 | 18 |
| 낙상 의심 | 25 |
| 정상 행동 | 34 |
| 합계 | 77 |

## 포함한 것

- `media/`: 기존 41개 + 추가 채택 36개를 원본 바이트 그대로 복사한 MP4. 변환하지 않았다.
- `media.json`: 모델 입력에 사용할 중립 파일 경로·해시·해상도·프레임률. 정답·원래 이름·판단 이유는 없다.
- `evaluation_labels.json`: 최신 3분류 정답, 원래 이유, 9개 보충 답변, 합의한 이유와 근거 한계 메모.
- `inventory.csv`: 사람이 확인할 전체 목록. 원래 이름, V 번호, SYN 번호를 연결한다.
- `source_records.json`: 이전 라벨·검토·위치/시각 자료·제외 기록 등의 출처 사본과 해시.
- `scoring_criteria_v2.json`, `scoring_criteria_v2_r1.json`: 처음 기준과 개정 기준의 원본 바이트.
- `validation.json`, `freeze.json`, `protocol.md`: 검증 결과, 고정 파일 해시, 사용 범위.

기존 SYN001~SYN041은 유지한다. 추가 영상은 V 순서대로 SYN042~SYN077을 부여했다.
제외된 V 번호는 재사용하지 않는다. V020→SYN060, V021→SYN061이며 원래 이름은 목록에서 확인할 수 있다.
모델 프롬프트에는 V 번호·원래 파일명·정답·판단 이유를 보내지 않는다.

## 확인 결과

- 77개 모두 존재하며 전체 7,010프레임 디코딩 통과. 원본·복사본 해시 일치.
- 파일 해시 중복 0개, 전체 디코딩 RGB가 완전히 같은 중복 0개.
- 같은 장면의 H3/LTX 변형 SYN005·SYN006은 별도 표시했다. 유사 영상이나 장면 독립성을 검증했다는 뜻은 아니다.
- 추가 품질 제외 V013·V036·V038이 들어가지 않았다. 기존 13·418의 제외/부재 이력도 보존했다.
- 77개 라벨·원래 이유가 최신 검토 기록과 일치. 보충 답변 9개, 근거 제한 메모 2개 보존.
- 기준 파일의 원본 해시 및 개정 기준이 참조하는 원본 해시 일치.
- 실제 묶음에 대한 무결성 재검사와 v2 입력 전 검사 통과. 관련 전체 테스트 633개 통과.
- 원본 영상·기존 라벨·이전 평가 결과는 변경하지 않았다.

묶음의 `full_dataset_finalized=true`는 이번 77개 **평가 입력 목록**이 확정됐다는 뜻이다.
이전 검토 문서의 `false`는 그때의 기록으로 보존하며, 모델 평가나 실물 검증 완료를 뜻하지 않는다.

## 다음 단계에서 주의할 점

- 기존 41개는 640×400·12fps, 추가 36개는 1280×720·24fps다. 비교 입력 조건은 다음 단계에서 정한다.
- 추가 36개에는 오디오 트랙이 있지만 RGB 평가 입력에는 사용하지 않는다.
- 기존 41개의 위치·시각 라벨은 같은 영상/프레임 기준임을 확인해 재사용했다. 추가 36개는 미라벨이다.
- 77개 전체의 영상 단위 후보 발생률·VLM 분류는 준비됐다. 전체 대상자 검출률·낙상 시작 지연은 준비되지 않았다.
  미라벨을 사람 없음/0초로 처리하지 않으며, 기존 전체 대상·시각 채점기는 이 묶음을 명시적으로 거부한다.
- 추론 시 `--dataset`과 `--frozen`은 모두 위 묶음 경로를 사용한다. 준비 코드는
  `scripts/prepare_fall_evaluation_v2.py`다. 평가 실행 코드의 Mac 반영은 다음 단계다.
- 모델 선정, 프레임 추출 조건, 응답 제한 시간 확정, 실제 모델 호출, 로봇 배포, 커밋·PR은 하지 않았다.

최종 묶음 해시: `334fadcf1fc576b81db21d5b9fd6e1dc8ab1625c76eae57162b2ec10adabab4f` (`freeze.json`).
기계용 완료 기록은 `evaluations/synthetic_fall_v1/dataset_freeze_v2_r1_receipt.json`에 있다.

중간 준비본은 같은 Mac의 `evaluation_sets/.preparation_history/fall77-b01/`에 보존했다.
기준 파일의 원본 바이트 보존을 보강하기 전 검사본이며, 이후 평가에는 위 최종 묶음만 사용한다.
